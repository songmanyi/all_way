代理模式，总共分为三种：
静态代理、动态代理指jdk代理、动态代理之cglib代理。

静态代理的话，主要是把被代理类和代理类共同实现一个接口。
然后被代理类，作为代理类的成员变量。并通过构造方法传参。

JDK代理：
实现的主要方式，是通过动态新建一个class类，和目标类共同实现一个接口。
并把目标作为代理类的成员变量。
代理类作为新类的成员变量。通过反射的方式，对方法进行调用。

cglib代理：
采用的是继承的方式。
首先，新建一个类，继承目标类，代理类通过callback的方式，传入到新类中。
新类中调用目标类方法时，实际上调用的时代理类中的intercept方法。