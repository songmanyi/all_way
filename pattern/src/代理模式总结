代理模式，总共分为三种：
静态代理、动态代理指jdk代理、动态代理之cglib代理。

静态代理的话，主要是把被代理类和代理类共同实现一个接口。
然后被代理类，作为代理类的成员变量。并通过构造方法传参。

JDK代理：
实现原理：会根据目标类接口方法，生成一个新的类，与目标类实现了同样的接口。
并且重写了所有接口方法，调用这些方法时，实际上时调用了InvocationHandler的invoke方法。
调用invoke方法时，传递了Method和参数，在invoke中通过反射，调用了目标类的方法。

cglib代理：
实现原理：
会根据目标类，实现一个继承目标类的子类。子类中有拦截器MethodInterceptor，调用子类的方法时，
也就是调用了拦截器的intercpt方法，调用intercpt方法时，会传递MethodProxy和参数，
通过FastClass机制，调用目标类中的方法。