原型模式，主要分为两种：
浅克隆和深克隆。

适用场景：
1、类初始化消耗的资源比较多。
2、new产生一个对象需要非常繁琐的过程。
3、构造方法比较复杂。
4、循环体中生产大量的对象。

浅克隆：
a、简单来说，就是创建一个带有clone方法的接口，通过实现这个接口，完成clone方法的实现。
缺点：引用对象仍然指向原来的对象。比如List成员变量。

深克隆：
a、实现Cloneable接口，包括成员变量。
b、实现序列化接口，通过反序列化的方式，生成对象。

