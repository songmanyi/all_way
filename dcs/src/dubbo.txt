1、dubbo的功能是服务治理，也是是PRPC框架下的一种实现。
    a、远程调用
    b、负载均衡

    支持的注册中心：
        a、zookeeper
        b、redis
        c、multicast
        d、simple
    
    支持的容器：
        a、Spring container
        b、Jetty container
        c、Log4j container

    多协议支持：
        RMI/hessian/webservice/http/thirft/Dubbo（默认）
    
    多注册中心