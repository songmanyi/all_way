1、值传递、引用传递？
2、装箱和拆分？

3、List和Set
    List 有序并且允许重复
    Set不允许重复并且无序

    ArrayList可变长度的动态数组
    原理：首先存储数据的，实际上就是一个数组。然后有三种初始化方式，一种是无参，则默认数组的长度是10个。一种是传递int，设置数组的长度。一种是传递collection，将数组的值，设置为collection的toArray。
     扩容：增加数据判断是否需要扩容，扩容1.5倍
     删除：就是数组右边的左移一位